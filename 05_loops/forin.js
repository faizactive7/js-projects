const myObj1 = {
    js: "JavaScript",
    Cpp: "C++",
    rb: "ruby",
    swift: "Swift by Apple"
}

for (const key in myObj1) {
    // console.log(`${key} shortcut is for ${myObj1[key]}`);
        
    }

const prog = ["Java", "Ruby", "Cpp"]

for (const key in prog) {
    // console.log(key);
    // console.log(prog[key]);
        
}

// const map = new Map()
// map.set('IN', "India")
// map.set('Fr', "France")
// map.set('UK', "United Kingdom")

// for (const key in map) {
//     console.log(key);               

// } cannot be iterated
